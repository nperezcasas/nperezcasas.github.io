/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    output: 'export',
    images: {
        unoptimized: true,
    },
  }
  
// next.config.js
module.exports = {
    output: 'export',
    basePath: '/nperezcasas.github.io',
    images: {
      unoptimized: true,
    },
  };